server {
        listen 80;

        server_name {{domain_name}};

        location /.well-known/ {
         root /srv/acme;
        }

        location / {
         return 301 https://{{domain_name}}$request_uri;
        }
}

